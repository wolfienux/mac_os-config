#! /usr/bin/env bash

# Installs applications.

install_zip_app "$COOLANT_APP_URL" "$COOLANT_APP_NAME"
install_dmg_app "$DOCKER_APP_URL" "$DOCKER_VOLUME_NAME" "$DOCKER_APP_NAME"
install_dmg_app "$CISCO_PACKET_TRACER_URL" "$CISCO_PACKET_TRACER_NAME"
